var capture1 = parseInt(document.getElementById('nb1').value);
var capture2 = parseInt(document.getElementById('nb2').value);
var capture3 = parseInt(document.getElementById('nb3').value);
var capture4 = parseInt(document.getElementById('nb4').value);
var valider = document.getElementById('valider');



function getSum() {
    return capture1 + capture2 + capture3 + capture4;
}

function getMin() {
    return Math.min(capture1, capture2, capture3, capture4);
}

function getMax() {
    return Math.max(capture1, capture2, capture3, capture4);
}

function getSquare[capture1, capture2, capture3, capture4] {
    return getSquare.map(x => x * 2);
}

valider.addEventListener('onclick', function () {
    if (document.getElementById('valider').checked) {
        return document.getElementById('somme').innerHTML = getSum;
        return document.getElementById('minimum').innerHTML = getMin;
        return document.getElementById('maximum').innerHTML = getMax;
        return document.getElementById('carre').innerHTML = getSquare;
    }
});


//document.getElementById('valider').checked)


