//var 

function getSum(capture1, capture2, capture3, capture4) {
    return capture1 + capture2 + capture3 + capture4;

}

function getMin() {
    return Math.min(capture1, capture2, capture3, capture4);
}

function getMax() {
    return Math.max(capture1, capture2, capture3, capture4);
}

function getSquare[capture1, capture2, capture3, capture4]{
    return getSquare.map(x => x * 2);
}





btnCalculer.addEventListener('Click', function () {
    return btnCalculer.innerHTML = afficher;
    return btnCalculer.innerHTML = minimum;
    return btnCalculer.innerHTML = maximum;
    return btnCalculer.innerHTML = carre1;
})

